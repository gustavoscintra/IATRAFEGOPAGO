import { PlaceholderPage } from '../../components/PlaceholderPage'

export default function ClientesPage() {
  return (
    <PlaceholderPage
      title="Clientes"
      desc="Sua carteira. Clique num cliente para ver a ficha, as tarefas dele e (em breve) o contrato."
      nextStep="Lista + ficha do cliente com CRUD completo entram no próximo passo (Fase 1.2)."
    />
  )
}
